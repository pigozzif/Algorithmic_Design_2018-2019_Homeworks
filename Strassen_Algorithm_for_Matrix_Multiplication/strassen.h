#ifndef __STRASSEN__

#include <stdlib.h>

void strassen(float **C, float **A, float **B, const size_t n);
void improved_strassen(float **C, float **A, float **B, const size_t n);

#endif // __STRASSEN__
